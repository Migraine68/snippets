{
	// Place your snippets for vba here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
	"Last Row Variable": {
		"prefix": "Lastrow",
		"body": [
			"Dim LROW as long  'lastrow variable",
			"LROW = Range(\"x\" & rows.count).end(xlup).row",
		],
		"description": "Variable that will be used to identify last row of data in a range"
		}
	, 
	// Code that runs when setting up a new sub in VBA
	"New Sub Setup": {
		"prefix": "Setup New Sub",
		"body": [
			"Dim WB as Workbook",
			"Dim WS as Worksheet",
			"Dim i as Long, n as Long, x as long 'Define counters",
			"",
			"Set WB = Activeworkbook",
			"Set WS = WB.Activesheet",
			],
		"description": "Setup the standard set of variables & object for new sub"
		}
}